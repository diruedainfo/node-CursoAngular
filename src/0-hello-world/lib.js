export function getGreetings(name) {
  return `Hello ${name}, welcome to the Javascript world!`;
}

export const empresa = "IGM";

export function getGreetings2(name) {
  return `Hello ${name}, welcome to the Javascript world!`;
}
